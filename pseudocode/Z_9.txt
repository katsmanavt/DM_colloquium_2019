//Z-9 

//Сергеев

//z1 — делимое, целое
//z2 — делитель, натуральное
//g — промежуточная переменная, натуральное
//r — результат деления, целое
// Ссылается на - DIV_NN_N, ABS_Z_N, POZ_Z_D, TRANS_N_Z, COM_NN_D, ADD_1N_N, MUL_ZM_Z

ФУНКЦИЯ DIV_ZZ_Z(z1, z2)
	// Нахождение неполного частного модуля делимого на делитель
	g := DIV_NN_N(ABS_Z_N(z1), ABS_Z_N(z2)) 
	// Если делимое положительно, 
	// преобразуем результат в целое цисло
	ЕСЛИ (POZ_Z_D (z1) = 2) 
		r := TRANS_N_Z (g)
	ИНАЧЕ
		// Иначе проверяем, не равно ли оно по модулю делителю
		ЕСЛИ (COM_NN_D (ABS_Z_N (z1), z2) != 0) 
			g := ADD_1N_N (g)
		КЕ
		r := TRANS_N_Z (g)
		r := MUL_ZM_Z (r)
	КЕ
	ВОЗВРАТ r		
КФ
