// Шубинов
// P - 8
// MUL_PQ_P, MUL_Pxk_P, ADD_PP_P

функция MUL_PP_P (многочлены с1, c2)
    /*
    c1, c2 – заданные многочлены, 
    с1.m, c2.m – их степени соответственно, 
    с – полученный многочлен,
    tmp - временная переменная.
    */
	c := многочлен()
	tmp := многочлен()
	ЦИКЛ ПО i ОТ 0 ДО с2.m
		// умножаем первый многочлен на коэф. 2-го многочлена,
		// результат храним в временной переменной.
		tmp := MUL_PQ_P(c1, c2[i])  
		//умножаем результат на x^i.
		tmp := MUL_Pxk_P(tmp,i)
		//прибавляем tmp к текущему значению многочлена.
		c := ADD_PP_P(c,tmp) 
	КЦ
	ВЕРНУТЬ c
КФ	
